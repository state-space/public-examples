import { type StateSpaceConfig } from "./config";
import { type Credentials } from "./util/auth";
export declare class Api {
    config: StateSpaceConfig;
    auth?: Credentials;
    constructor(config: StateSpaceConfig);
    init(): Promise<this>;
    requestExploration(autorun?: boolean, force?: boolean): Promise<{
        id?: string | undefined;
        error: boolean;
        message: string;
    } | undefined>;
    status(_id: string): Promise<void>;
    setAuth(creds: Credentials): Promise<void>;
}
//# sourceMappingURL=api.d.ts.map